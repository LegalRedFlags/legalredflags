import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "36 Lawyer Red Flags: Complete Database from Real Cases",
  description:
    "Explore our non-profit database of 36 attorney red flags across hiring, retainer fees, and misconduct — evidence-backed, not theory.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
